name: urban_leaf
description: "UrbanLeaf AI — Smart Urban Farming Assistant"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.12.0-247.0.dev

dependencies:
  firebase_auth: ^6.5.1
  cloud_firestore: ^6.4.1
  firebase_core: ^4.9.0
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  image_picker: ^1.2.2
  intl: ^0.19.0
  share_plus: ^10.1.4
  tflite_v2: ^1.0.0
  image: ^4.3.0
  camera: ^0.12.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/models/
